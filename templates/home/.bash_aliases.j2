#!/bin/sh
# {{ ansible_managed }}
{% for item in (skel_default_bash_aliases| combine(skel_bash_aliases) | combine(item.0.bash_aliases|default({})) ) |dictsort %}
alias {{ item.0 }}='{{ item.1 }}'
{% endfor %}
